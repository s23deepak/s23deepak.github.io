<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://s23deepak.github.io/feed.xml" rel="self" type="application/atom+xml" /><link href="https://s23deepak.github.io/" rel="alternate" type="text/html" /><updated>2025-12-22T07:06:07+00:00</updated><id>https://s23deepak.github.io/feed.xml</id><title type="html">AI Blog</title><subtitle>Exploring the world of AI, one learning at a time.</subtitle><entry><title type="html">My Understanding Of Supervised Reinforcement Learning</title><link href="https://s23deepak.github.io/2025/12/22/my-understanding-of-supervised-reinforcement-learning.html" rel="alternate" type="text/html" title="My Understanding Of Supervised Reinforcement Learning" /><published>2025-12-22T00:00:00+00:00</published><updated>2025-12-22T00:00:00+00:00</updated><id>https://s23deepak.github.io/2025/12/22/my-understanding-of-supervised-reinforcement-learning</id><content type="html" xml:base="https://s23deepak.github.io/2025/12/22/my-understanding-of-supervised-reinforcement-learning.html"><![CDATA[<h1 id="supervised-reinforcement-learning-srl-fine-tuning-reasoning-models-step-by-step">Supervised Reinforcement Learning (SRL): Fine-Tuning Reasoning Models Step by Step</h1>

<p>Training language models for <strong>complex, multi-step reasoning</strong> remains a fundamental challenge. In such tasks, a single incorrect intermediate step can derail the entire reasoning chain—even if many parts of the solution are correct. Unfortunately, most standard fine-tuning approaches fail to handle this gracefully.</p>

<p>Here I share my understanding of <strong>Supervised Reinforcement Learning (SRL)</strong>, a fine-tuning framework proposed by Google, which reframes reasoning as a <strong>step-wise decision-making problem</strong> and provides dense, stable supervision for learning structured reasoning.</p>

<hr />

<h2 id="why-standard-fine-tuning-struggles-with-reasoning">Why Standard Fine-Tuning Struggles with Reasoning</h2>

<h3 id="limitations-of-supervised-fine-tuning-sft">Limitations of Supervised Fine-Tuning (SFT)</h3>

<p>Supervised Fine-Tuning (SFT) trains models using next-token prediction over full expert solutions. While effective for surface-level imitation, SFT enforces <strong>rigid token-level copying</strong>:</p>

<ul>
  <li>The model must reproduce the <em>entire</em> expert trajectory exactly</li>
  <li>Any deviation—even if logically valid—is penalized</li>
  <li>Generalization beyond the training data is limited</li>
</ul>

<h3 id="limitations-of-outcome-based-rl-rlvr--grpo">Limitations of Outcome-Based RL (RLVR / GRPO)</h3>

<p>Outcome-based reinforcement learning methods, such as RL with verifiable rewards (RLVR), optimize only for the <strong>final answer</strong>:</p>

<ul>
  <li>Intermediate reasoning is ignored</li>
  <li>Partially correct solutions still receive zero or negative reward</li>
  <li>Sparse rewards lead to unstable training</li>
  <li>Long-horizon reasoning problems become difficult to explore</li>
</ul>

<p>As a result, many complex reasoning tasks become effectively <strong>intractable</strong> under purely outcome-based objectives.</p>

<hr />

<h2 id="core-idea-of-supervised-reinforcement-learning-srl">Core Idea of Supervised Reinforcement Learning (SRL)</h2>

<p>SRL reframes problem solving as a <strong>sequential decision-making process</strong>.</p>

<p>Instead of:</p>
<ul>
  <li>Imitating the entire expert trajectory (SFT), or</li>
  <li>Optimizing only the final outcome (RLVR),</li>
</ul>

<p><strong>SRL trains the model to reproduce the sequence of key reasoning actions</strong> underlying expert solutions, using an RL-style objective with <strong>step-wise supervision</strong>.</p>

<p>Key ideas:</p>
<ul>
  <li>Expert demonstrations are decomposed into <strong>meaningful reasoning steps</strong></li>
  <li>Each step is treated as an <strong>action</strong></li>
  <li>The model receives a <strong>reward at every step</strong>, not just at the end</li>
</ul>

<hr />

<h2 id="how-srl-works-high-level-overview">How SRL Works (High-Level Overview)</h2>

<p>The figure below illustrates SRL’s step-wise training procedure, where partial trajectories are used to supervise each reasoning step independently.</p>

<p><img src="../assets/images/SRL%20step-wise.png" alt="SRL Step-Wise Training" /></p>

<p><em>Image generated using Google’s Nano Banana</em></p>

<p>At each step <em>k</em>, the policy model generates two components:</p>

<ol>
  <li>An internal monologue, explicitly encapsulated within <code class="language-plaintext highlighter-rouge">&lt;think&gt;...&lt;/think&gt;</code> tags.</li>
  <li>A step action <em>âₖ</em>, representing the model’s external reasoning decision.</li>
</ol>

<p>While the internal monologue is not supervised or compared to the teacher, the <code class="language-plaintext highlighter-rouge">&lt;think&gt;</code> tag enforces a clear separation between private reasoning and externally evaluated actions. Only the step action contributes to the reward signal.</p>

<hr />

<h2 id="comparing-training-paradigms">Comparing Training Paradigms</h2>

<table>
  <thead>
    <tr>
      <th>Method</th>
      <th>Needs Teacher Demonstration</th>
      <th>Reward Type</th>
      <th>What It Trains</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>SFT</strong></td>
      <td>Yes</td>
      <td>None</td>
      <td>Full trajectory imitation</td>
    </tr>
    <tr>
      <td><strong>SRL</strong></td>
      <td>Yes</td>
      <td>Step-wise similarity</td>
      <td>Reasoning steps</td>
    </tr>
    <tr>
      <td><strong>RLVR / GRPO</strong></td>
      <td>No</td>
      <td>Final verifiable reward</td>
      <td>Outcome correctness</td>
    </tr>
  </tbody>
</table>

<hr />

<h2 id="step-wise-training-data-construction">Step-Wise Training Data Construction</h2>

<p>SRL uses a powerful <strong>teacher model</strong> θ_expert to generate expert solution trajectories.</p>

<p>Given a solution with <strong>N reasoning steps</strong>, SRL constructs <strong>N − 1 partial trajectories</strong>.<br />
For each step <em>k</em> ∈ {1, …, N-1}:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>x_step_k = [x, y_step_1, ..., y_step_{k-1}]
</code></pre></div></div>

<p>The model is trained to predict: <strong>y_step_k</strong></p>

<p>This transforms a single expert solution into many training examples that teach the model how to proceed correctly from different intermediate states.</p>

<hr />

<h2 id="supervising-reasoning-steps-not-final-answers">Supervising Reasoning Steps (Not Final Answers)</h2>

<p>The teacher trajectory is converted into a sequence of <strong>actions</strong>:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>a₁, a₂, a₃, ..., aₖ
</code></pre></div></div>

<p>Each <em>aₖ</em> represents a meaningful reasoning decision.</p>

<p>At step <em>k</em>, the policy model is given:</p>
<ul>
  <li>The original query</li>
  <li>The context of all previous steps <em>a₁ … aₖ₋₁</em></li>
</ul>

<p>The model generates:</p>
<ol>
  <li>An internal reasoning trace (kept private)</li>
  <li>A <strong>step action</strong> <em>âₖ</em></li>
</ol>

<p>Only the action is compared with the teacher’s action.<br />
The internal reasoning remains free and unsupervised.</p>

<hr />

<h2 id="step-wise-reward-via-sequence-similarity">Step-Wise Reward via Sequence Similarity</h2>

<p>For each step <em>k</em>:</p>

<ul>
  <li>Model action: <em>âₖ</em></li>
  <li>Teacher action: <em>aₖ</em></li>
</ul>

<p>The reward is computed using a <strong>sequence similarity function</strong>:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>rₖ = R(ŷ_step_k, y_step_k) = 2M / T
</code></pre></div></div>

<p>Where:</p>
<ul>
  <li><strong>T</strong> = total number of elements in both sequences combined</li>
  <li><strong>M</strong> = total number of elements in all non-overlapping matching blocks</li>
</ul>

<p>The similarity is computed by first identifying the longest contiguous matching subsequence and then recursively searching the remaining segments.</p>

<p>Although SRL provides supervised, step-wise rewards, optimization is performed using a reinforcement learning objective. Specifically, the paper employs Group Relative Policy Optimization (GRPO) with a KL-divergence penalty that constrains the policy from drifting too far from a reference model.</p>

<p>This combination allows SRL to benefit from dense supervision while retaining the stability guarantees of modern RL fine-tuning methods.</p>

<h3 id="format-enforcement">Format Enforcement</h3>

<p>If the model output fails to follow the required format, a negative reward is assigned:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>r(ŷ, y) = R(ŷ, y)   if output follows format
        = -1        otherwise
</code></pre></div></div>

<hr />

<h2 id="dynamic-sampling-for-srl">Dynamic Sampling for SRL</h2>

<p>Not all training samples provide useful learning signals.</p>

<p>If all rollouts for a sample yield rewards with near-zero variance, the resulting advantage is weak. SRL therefore filters such samples and retains a sample only if:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>std({r₁, r₂, ...}) &gt; ε
</code></pre></div></div>

<p>This ensures training focuses on examples that meaningfully distinguish correct and incorrect reasoning behavior.</p>

<hr />

<h2 id="practical-insights-from-the-authors">Practical Insights from the Authors</h2>

<h3 id="out-of-order-reasoning-steps">Out-of-Order Reasoning Steps</h3>

<p>What if the model uses a different but correct reasoning order?</p>

<p>In some tasks, multiple reasoning orders may be logically valid (e.g., A → B → C vs. B → A → C).</p>

<p><strong>Insights from the authors:</strong></p>
<ul>
  <li>Providing <strong>step titles or step-level prompts</strong> from the teacher helps align student behavior</li>
  <li>Using <strong>answer augmentation</strong>, where multiple teacher trajectories are generated per step, further improves robustness</li>
  <li>The reward is taken as the <strong>maximum similarity score across teacher variants</strong>, allowing flexibility in reasoning order</li>
</ul>

<hr />

<h3 id="combining-intermediate-and-final-rewards">Combining Intermediate and Final Rewards</h3>

<p>The authors explored combining step-wise SRL rewards with final outcome rewards (RLVR). However, this introduced the challenge of <strong>reward balancing</strong>.</p>

<p><strong>Final approach used in the paper:</strong></p>
<ol>
  <li>Train the model using <strong>SRL</strong> to learn structured reasoning</li>
  <li>Fine-tune using <strong>RLVR</strong> to optimize final outcomes</li>
</ol>

<p>This staged strategy avoids instability while preserving both step-wise correctness and goal achievement.</p>

<hr />

<h2 id="key-takeaways">Key Takeaways</h2>

<ul>
  <li>SRL reframes reasoning as <strong>sequential decision-making</strong></li>
  <li>It provides <strong>dense, stable supervision</strong> at every reasoning step</li>
  <li>Internal reasoning is free and not directly supervised</li>
  <li>Training scales efficiently and generalizes better than SFT</li>
  <li>A practical pipeline is <strong>SRL → RLVR</strong></li>
</ul>

<p>SRL bridges the gap between imitation learning and outcome-based reinforcement learning, making it a powerful framework for training reasoning-capable language models.</p>

<h2 id="references">References</h2>
<p><a href="https://doi.org/10.48550/arXiv.2510.25992">Supervised Reinforcement Learning: From Expert Trajectories to Step-wise Reasoning.</a></p>]]></content><author><name></name></author><summary type="html"><![CDATA[Supervised Reinforcement Learning (SRL): Fine-Tuning Reasoning Models Step by Step]]></summary></entry><entry><title type="html">How vLLM Solves Out-Of-Memory Errors in LLMs</title><link href="https://s23deepak.github.io/2025/11/14/how-vllm-solves-out-of-memory-errors-in-llms.html" rel="alternate" type="text/html" title="How vLLM Solves Out-Of-Memory Errors in LLMs" /><published>2025-11-14T00:00:00+00:00</published><updated>2025-11-14T00:00:00+00:00</updated><id>https://s23deepak.github.io/2025/11/14/how-vllm-solves-out-of-memory-errors-in-llms</id><content type="html" xml:base="https://s23deepak.github.io/2025/11/14/how-vllm-solves-out-of-memory-errors-in-llms.html"><![CDATA[<h1 id="how-vllm-solves-out-of-memory-errors-in-llms">How vLLM Solves Out-Of-Memory Errors in LLMs</h1>

<h2 id="understanding-the-problem-kv-cache-memory-challenges">Understanding the Problem: KV Cache Memory Challenges</h2>

<p>During LLM inference, the model stores key-value (KV) representations of all previously generated tokens in memory. As sequences generate more tokens, this KV cache grows dynamically, creating significant memory management challenges that traditional systems struggle to handle efficiently.</p>

<h3 id="three-types-of-memory-waste">Three Types of Memory Waste</h3>

<figure>
	<img src="../../../../assets/images/memory_waste.png" alt="Memory waste illustration" style="max-width:100%;height:auto;display:block;margin:0.5rem 0;" />
	<figcaption style="color:#666;font-size:0.9rem;margin-bottom:1rem;">Illustration of memory waste types</figcaption>
</figure>

<p>Traditional memory management for LLM inference suffers from three critical inefficiencies:</p>

<p><strong>1. Internal Fragmentation</strong><br />
Systems must pre-allocate memory slots for the maximum possible sequence length since the final output length is unknown in advance. When sequences end earlier than expected, the unused pre-allocated space is wasted.</p>

<p><strong>2. Reservation Overhead</strong><br />
Memory slots are allocated but remain idle while waiting for future tokens to be generated. This happens because traditional systems require contiguous memory blocks for each sequence, forcing early allocation of space that may not be immediately needed.</p>

<p><strong>3. External Fragmentation</strong><br />
Different requests have varying sequence lengths, creating unusable gaps between allocated memory blocks. These gaps cannot be efficiently utilized by other sequences, leading to wasted memory capacity.</p>

<h2 id="the-solution-pagedattention">The Solution: PagedAttention</h2>

<figure>
	<img src="../../../../assets/images/pagedattention_solution.png" alt="PagedAttention solution" style="max-width:100%;height:auto;display:block;margin:0.5rem 0;" />
	<figcaption style="color:#666;font-size:0.9rem;margin-bottom:1rem;">PagedAttention: KV blocks and block tables</figcaption>
</figure>

<p>vLLM introduces PagedAttention, which fundamentally reimagines how KV cache memory is managed. Instead of requiring contiguous memory blocks, PagedAttention partitions the KV cache into fixed-size blocks, allowing sequences to occupy non-contiguous memory locations—similar to how operating systems manage virtual memory.</p>

<h3 id="core-components">Core Components</h3>

<p><strong>KV Blocks</strong><br />
The KV cache is divided into fixed-size blocks (typically 16-32 tokens each), analogous to memory pages in operating systems. Each block stores key-value vectors for a fixed number of tokens.</p>

<p><strong>Block Tables</strong><br />
Each sequence maintains a block table that maps logical blocks to physical memory blocks, similar to page tables in OS memory management. This mapping enables non-contiguous storage while maintaining logical sequence ordering.</p>

<p><strong>On-Demand Allocation</strong><br />
Physical blocks are allocated only when needed as sequences grow, rather than pre-allocating the maximum possible length. When a logical block fills up, vLLM allocates a new physical block from the free pool on demand.</p>

<h3 id="how-it-works-in-practice">How It Works in Practice</h3>

<p>During attention computation, PagedAttention efficiently fetches KV blocks from arbitrary memory positions and patches them together for processing. The algorithm operates purely at the memory management level without requiring any model architecture changes.</p>

<p>For example, consider the prompt “Alan Turing is a computer scientist and mathematician.” This gets partitioned into logical blocks like “Alan Turing is a” and “computer scientist and mathematician.” These logical blocks map to physical blocks that may be scattered across GPU memory. As the model generates new tokens, they’re appended to existing blocks (like “renowned”) or trigger allocation of new blocks on demand.</p>

<h2 id="cpu-offloading-of-model-weights">CPU offloading of model weights</h2>
<p>The CPU offloading technique allows parts of the model weights to be kept in CPU memory and streamed to the GPU on demand during inference, effectively expanding usable GPU memory capacity virtually.</p>

<h3 id="how-cpu-offloading-works-in-vllm">How CPU Offloading Works in vLLM</h3>
<p>When GPU VRAM fills up, parts of the model weights that are not instantly needed are moved to CPU memory.
During a forward pass, required weights are moved back to GPU for computation and then possibly offloaded back to CPU.
This streaming of weights between CPU and GPU happens layer-wise and dynamically to maximize memory usage without crashing due to out-of-memory errors.
Note : Increasing CPU offloading for model weights can free GPU VRAM for KV cache but does not directly offload the KV cache itself.</p>
<h3 id="practical-summary">Practical Summary</h3>
<p>CPU offloading in vLLM helps when the total model + KV cache size exceeds GPU memory by offloading model weights to CPU memory dynamically.
The KV cache needs to fit on the GPU, so very large KV caches risk out-of-memory even with CPU offload.</p>

<h2 id="prevention-of-out-of-memory-errors">Prevention of Out-of-Memory Errors</h2>
<p><strong>Dynamic Resource Management</strong><br />
Instead of failing when a single large contiguous block isn’t available, vLLM can utilize any available blocks scattered across memory. The system dynamically trades sequence length for batch size based on actual available memory.</p>

<p><strong>Minimal Fragmentation</strong><br />
Internal fragmentation is bounded by block size (16-32 tokens) rather than maximum sequence length. External fragmentation is eliminated entirely since all blocks have uniform size.</p>

<p><strong>Predictable Memory Usage</strong><br />
The total physical cache memory remains statically allocated, providing protection against runtime OOM errors. The scheduler intelligently manages which requests advance based on available blocks in the free pool.</p>

<p><strong>Efficient Cleanup</strong><br />
When sequences complete, their blocks immediately return to the free pool for reuse by other requests, maximizing memory utilization.</p>

<p><strong>Virtual Memory Expansion</strong><br />
CPU offloading allows model weights to be stored in CPU memory and streamed to GPU on demand, virtually expanding GPU capacity beyond physical VRAM limits.</p>

<p><strong>Dynamic Weight Streaming</strong><br />
Weights are moved between CPU and GPU dynamically during inference, layer by layer, to ensure only necessary weights are in GPU memory at any time.</p>

<p><strong>Complementary Memory Management</strong><br />
By offloading model weights, CPU offloading frees GPU VRAM for KV cache management, working alongside PagedAttention to prevent OOM errors in larger models.</p>

<p>Photos courtesy of <a href="https://www.youtube.com/watch?v=5ZlavKF_98U">Fast LLM Serving with vLLM and PagedAttention</a></p>]]></content><author><name></name></author><summary type="html"><![CDATA[How vLLM Solves Out-Of-Memory Errors in LLMs]]></summary></entry><entry><title type="html">AlphaZero with JAX-based Implementation</title><link href="https://s23deepak.github.io/2025/09/15/alphazero-with-jax-based-implementation.html" rel="alternate" type="text/html" title="AlphaZero with JAX-based Implementation" /><published>2025-09-15T00:00:00+00:00</published><updated>2025-09-15T00:00:00+00:00</updated><id>https://s23deepak.github.io/2025/09/15/alphazero-with-jax-based-implementation</id><content type="html" xml:base="https://s23deepak.github.io/2025/09/15/alphazero-with-jax-based-implementation.html"><![CDATA[<h1 id="alphazero-with-jax-based-implementation">AlphaZero with JAX-based Implementation</h1>

<p>This directory contains an implementation of AlphaZero for a custom turn-based strategy game, using <a href="https://jax.readthedocs.io/en/latest/">JAX</a> for high-performance computation and parallelization. Monte Carlo Tree Search (MCTS) is combined with a convolutional neural network (CNN) for policy and value prediction. JAX is used to accelerate neural network operations and enable efficient parallelization across devices like GPUs and TPUs.</p>

<h2 id="how-it-works">How It Works</h2>

<ul>
  <li><strong>JAX</strong> provides just-in-time (JIT) compilation and automatic differentiation to speed up computations and enable efficient batching and parallelization.</li>
  <li><strong>MCTX</strong> By <a href="https://github.com/google-deepmind/mctx">DeepMind</a>, is Monte Carlo Tree Search implementation in JAX</li>
  <li><strong>Neural network inference and training</strong> are performed on the GPU/TPU using JAX’s optimized primitives.</li>
</ul>

<h3 id="jax-jit-and-vectorization">JAX JIT and Vectorization</h3>

<ul>
  <li><code class="language-plaintext highlighter-rouge">@jax.jit</code>: Compiles the function for efficient execution on CPU/GPU/TPU, eliminating Python overhead.</li>
  <li><code class="language-plaintext highlighter-rouge">jax.vmap</code>: Automatically vectorizes the function to handle batches of inputs efficiently, enabling parallel processing of multiple game states.</li>
</ul>

<h4 id="effect-on-utilization">Effect on Utilization</h4>

<ul>
  <li><strong>CPU Utilization</strong>: MCTS remains CPU-bound, but JAX’s JIT compilation reduces overhead in neural network calls.</li>
  <li><strong>GPU/TPU Utilization</strong>: JAX enables high utilization by compiling operations to optimized kernels and supporting batch processing. Multiple MCTS simulations can be batched together for inference, improving hardware efficiency.</li>
  <li><strong>Parallelization</strong>: JAX’s <code class="language-plaintext highlighter-rouge">pmap</code> can distribute computations across multiple devices if available, further increasing throughput.</li>
</ul>

<p><strong>Note:</strong> JAX’s functional programming paradigm requires careful state management and <a href="https://docs.jax.dev/en/latest/notebooks/Common_Gotchas_in_JAX.html#pure-functions">pure functions</a>, but it provides significant performance gains for numerical computations in AlphaZero.</p>

<h2 id="observations">Observations</h2>

<ul>
  <li><strong>GPU/TPU Utilization</strong>: JAX maximizes hardware utilization through compilation and vectorization, making neural network operations much faster than in pure NumPy implementations.</li>
  <li><strong>Scalability</strong>: JAX’s design allows for easy scaling to multiple GPUs or TPUs, providing better performance than CPU-only implementations.</li>
</ul>]]></content><author><name></name></author><summary type="html"><![CDATA[AlphaZero with JAX-based Implementation]]></summary></entry></feed>